server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/truongle2004/tech_shop_config
          default-label: main
          clone-on-start: true
